// Native CSS Scroll Snap Architecture
// Lenis smooth scroll is disabled to ensure 100% deterministic native CSS scroll snap across all viewports

export function initSmoothScroll(): () => void {
  return () => {};
}

export function getLenis(): null {
  return null;
}
