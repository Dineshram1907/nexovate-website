import React from "react";

/**
 * CustomCursor (Deprecated / Disabled)
 *
 * Native OS pointer is restored across all platforms.
 * Retained as a null component for backward compatibility.
 */
export const CustomCursor: React.FC = () => null;

export default CustomCursor;
